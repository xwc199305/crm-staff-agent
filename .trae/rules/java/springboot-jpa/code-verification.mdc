---
description: Java 代码生成后的验证规范，要求在生成和编译代码后必须启动应用进行运行验证。
globs: **/*.java
---
- 在生成或修改 Java 代码后，必须执行完整的验证流程：编译 → 启动 → 测试。
- 编译验证：使用 `mvn clean compile` 确保代码能够成功编译。
- 启动验证：使用 `mvn spring-boot:run` 启动 Spring Boot 应用，验证应用能够正常启动。
- 启动后等待至少 5-10 秒，确保应用完全启动。
- 通过检查控制台输出确认应用启动成功（查找 "Started Application in" 等关键日志）。
- 如果应用启动失败，分析错误日志并修复问题，然后重新验证。
- 在完成所有验证并确认应用正常运行后，才能结束任务。
- 对于无法启动的场景（如缺少外部依赖如数据库），需记录原因并提供替代验证方案。